  <footer class="mt-auto bg-gray-800 text-gray-300">
    <div class="container mx-auto px-4 py-8">
      <div class="flex flex-col md:flex-row justify-between items-center">
        <div class="mb-4 md:mb-0 flex items-center space-x-2">
          <div class="bg-white text-primary-600 h-8 w-8 rounded-full flex items-center justify-center">
            <i class="fas fa-cloud-upload-alt"></i>
          </div>
          <p>© <%= new Date().getFullYear() %> Image Cloud. All rights reserved.</p>
        </div>
        <div class="flex space-x-4">
          <a href="#" class="hover:text-white transition duration-200 h-8 w-8 rounded-full bg-gray-700 flex items-center justify-center hover:bg-gray-600">
            <i class="fab fa-github"></i>
          </a>
          <a href="#" class="hover:text-white transition duration-200 h-8 w-8 rounded-full bg-gray-700 flex items-center justify-center hover:bg-gray-600">
            <i class="fab fa-twitter"></i>
          </a>
          <a href="#" class="hover:text-white transition duration-200 h-8 w-8 rounded-full bg-gray-700 flex items-center justify-center hover:bg-gray-600">
            <i class="fab fa-linkedin"></i>
          </a>
        </div>
      </div>
    </div>
  </footer>
  
  <!-- Notification Component -->
  <div id="notification" class="fixed bottom-4 right-4 px-6 py-3 rounded-md shadow-lg hidden transform transition-all duration-300 translate-y-2 opacity-0 z-50"></div>

  <script>
    // Mobile menu toggle
    document.addEventListener('DOMContentLoaded', function() {
      const mobileMenuButton = document.querySelector('.mobile-menu-button');
      const mobileMenu = document.querySelector('.mobile-menu');
      
      if (mobileMenuButton && mobileMenu) {
        mobileMenuButton.addEventListener('click', function() {
          mobileMenu.classList.toggle('hidden');
        });
      }
    });
    
    // Notification function
    function showNotification(message, type = 'success') {
      const notification = document.getElementById('notification');
      notification.textContent = message;
      
      // Set color based on type
      if (type === 'success') {
        notification.className = 'fixed bottom-4 right-4 px-6 py-3 rounded-md shadow-lg bg-green-500 text-white text-sm z-50 transform transition-all duration-300';
      } else if (type === 'error') {
        notification.className = 'fixed bottom-4 right-4 px-6 py-3 rounded-md shadow-lg bg-red-500 text-white text-sm z-50 transform transition-all duration-300';
      } else {
        notification.className = 'fixed bottom-4 right-4 px-6 py-3 rounded-md shadow-lg bg-blue-500 text-white text-sm z-50 transform transition-all duration-300';
      }
      
      // Show notification
      setTimeout(() => {
        notification.classList.remove('hidden', 'translate-y-2', 'opacity-0');
      }, 10);
      
      // Hide notification
      setTimeout(() => {
        notification.classList.add('translate-y-2', 'opacity-0');
        setTimeout(() => {
          notification.classList.add('hidden');
        }, 300);
      }, 3000);
    }
  </script>
</body>
</html>
